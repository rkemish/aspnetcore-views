@model FeatureCardViewModel

<article class="card h-100 shadow-sm">
    <div class="card-body">
        <span class="badge text-bg-primary mb-2">@Model.BadgeText</span>
        <h3 class="h5">@Model.Title</h3>
        <p class="mb-0">@Model.Description</p>
    </div>
</article>
